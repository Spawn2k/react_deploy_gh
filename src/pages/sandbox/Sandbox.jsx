import React, { useEffect } from 'react';

import PageNotFound from '../../components/PageNotFound';

const Sandbox = (props) => {
  // const {} = props;

  return (
    <div className="m-sandbox">
      <PageNotFound />
    </div>
  );
};

export default Sandbox;
